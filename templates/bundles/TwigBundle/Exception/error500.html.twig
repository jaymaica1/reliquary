{% extends 'landing_base.html.twig' %}

{% block title %}{{ 'error.server_error.title'|trans({}, 'error') }}{% endblock %}

{% block body %}
<div class="container py-5" style="margin-top: 100px;">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <img src="{{ asset('/images/relic.png') }}" width="100" height="100" class="mb-4" alt="{{ 'error.logo_alt'|trans({}, 'error') }}">
            <h1 class="display-4 mb-4" style="font-family: 'Cinzel', serif;">{{ 'error.server_error.heading'|trans({}, 'error') }}</h1>
            <div class="card mb-4" style="background: rgba(255, 255, 255, 0.9); border: 1px solid var(--accent-gold);">
                <div class="card-body">
                    <p class="lead">{{ 'error.server_error.oops'|trans({}, 'error') }}</p>
                    <p>{{ 'error.server_error.technical_difficulties'|trans({}, 'error') }}</p>
                    <p>{{ 'error.server_error.try_later'|trans({}, 'error') }}</p>
                </div>
            </div>
            <div class="d-grid gap-3 d-sm-flex justify-content-sm-center">
                <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg px-4 gap-3">
                    <i class="fas fa-home me-2"></i>
                    {{ 'error.return_home'|trans({}, 'error') }}
                </a>
                <button onclick="window.location.reload()" class="btn btn-outline-secondary btn-lg px-4">
                    {{ 'error.server_error.try_again'|trans({}, 'error') }}
                </button>
                {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin_logs') }}" class="btn btn-outline-danger btn-lg px-4">
                    <i class="fas fa-terminal me-2"></i>
                    {{ 'error.view_logs'|trans({}, 'error') }}
                </a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}