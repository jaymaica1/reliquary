{% extends 'landing_base.html.twig' %}

{% block title %}{{ 'relic.new_title'|trans({}, 'relic') }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/relic-form.css') }}">
{% endblock %}

{% block body %}
<section class="list">
    <div class="relic-form-container">
        <h2 class="section-title">{{ 'relic.add_new'|trans({}, 'relic') }}</h2>
        <p class="section-subtitle">{{ 'relic.add_subtitle'|trans({}, 'relic') }}</p>
        
        {{ include('relic/_form.html.twig') }}
    </div>
</section>
{% endblock %}
